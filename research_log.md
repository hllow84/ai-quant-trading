# Research Log

All tested strategies and factors. Updated every session.

| date | strategy | instrument | timeframe | key params | result (PF/Sharpe/verdict) | notes |
|------|----------|------------|-----------|------------|----------------------------|-------|
| 2026-06-14 | ICT SMC Full Model (FTMO) | XAUUSD | H4 | sweep→OB/FVG→kill-zone entry, 1.5R TP, SL below OB, RR 1.5 | PF 2.19, 49 trades | Gold-only filter applied; weak absolute return given low trade count; needs spread + FTMO rule simulation |
| 2026-06-14 | ICT SMC Full Model (FTMO) | EURUSD | H4 | same params, pc_sym=EURUSD default | PF 0.98 | Below breakeven after spread; no edge on EURUSD H4 |
| 2026-06-14 | ICT SMC Full Model (FTMO) | EURUSD | H1 | same model, H1 bar | PF 0.58 | Worse than H4; finer timeframe amplifies noise |
| 2026-07-10 | SMA-200 long-only baseline | XAUUSD | Daily | window=200, no params to tune | Net Sharpe 0.665, Gross Sharpe 0.673, MDD ~27%, n_configs=1 | yfinance GC=F (futures) used — PROVISIONAL/SUPERSEDED; replaced by real SPOT run below. Cost: 4 bps fee. Look-ahead guard PASS. |
| 2026-07-17 | SMA-200 long-only baseline (REAL SPOT) | XAUUSD | Daily | window=200, no params to tune | Net Sharpe **1.127** (Gross 1.135), MDD 26.2%, CAGR 17.6%, 19 entries, n_configs=1 | Real Dukascopy M1 SPOT → daily (1,602 bars, 2018-2025). Real spread cost @ close (median 2.15 bps round-turn, LOWER than provisional 4 bps). Look-ahead PASS. **KEY: loses to buy-and-hold gold on EVERY metric** — B&H Sharpe 1.194, MDD 20.4%, CAGR 20.7%. SMA-200 timing overlay DESTROYS value (higher DD despite 66.5% exposure — whipsaws back in near tops). Both are pure long-gold beta in a secular bull, not alpha. |
| 2026-07-18 | A: Trend-continuation (H1 trend, M15 pullback) | XAUUSD | M15/H1 | 1% risk, 2R target, no tuning, session 07-20 UTC flat 21 | **KILL.** netPF 0.69, gross PF 0.95, Sharpe -3.06, MaxDD 99.1%, 2086 trades. FTMO P1 pass 2.1% | Real spread + $0.07/oz comm + news slippage. Guard PASS. **No edge even before costs** (gross R -0.027/trade). n_configs=3 DSR moot (all-negative Sharpes). |
| 2026-07-18 | B: Breakout-retest (London opening range) | XAUUSD | M15 | 1% risk, 2R target, OR 07-08 UTC, entries 08-16 flat 20, no tuning | **KILL.** netPF 0.67, gross PF 1.05, Sharpe -3.28, MaxDD 99.6%, 1916 trades. FTMO P1 pass 2.1% | Tiny gross edge (+0.032R/trade) but cost 0.31R/trade destroys it. Stops too tight (~1.8 $/oz): **commission alone ($0.07/oz) exceeds the entire gross edge (0.057 $/oz).** Guard PASS. |
| 2026-07-18 | C: Mean-reversion at prior-day H/L | XAUUSD | M15 | 1% risk, 1.5R target, session 07-20 UTC flat 21, no tuning | **KILL.** netPF 0.69, gross PF 1.11, Sharpe -2.08, MaxDD 88.4%, 945 trades. FTMO P1 pass 3.2% | Best raw edge of the three (+0.058R/trade) but cost 0.27R/trade kills it. Least-bad loser. Guard PASS. |
| 2026-07-18 | A: Swing trend-continuation (Daily trend, H4 pullback) | XAUUSD | H4/D | 1% risk, 2R, multi-day hold, no tuning | **KILL.** grPF 1.01, netPF 0.95, Sharpe -0.19, DSR 0.00, MaxDD 21.3%, 168 trades | medR 14.5$/oz → **cost_R 4.1% (was ~30% intraday — cost problem SOLVED)**. But gross edge ~0 (+0.007R/trade). Loses to B&H gold (SR 1.19). Guard PASS. |
| 2026-07-18 | B: Swing breakout-retest (prior-week range, H4 retest) | XAUUSD | H4/W | 1% risk, 2R, stop 1×ATR_H4 beyond level, multi-day, no tuning | **KILL (near-breakeven).** grPF 1.05, netPF 0.98, Sharpe -0.05, DSR 0.46, MaxDD 11.0%, 173 trades | medR 12$/oz → cost_R 4.4%. Least-bad: real but tiny gross edge (+0.031R), net just -0.013R. Lowest DD of all. Still loses to B&H. Guard PASS. |
| 2026-07-18 | C: Swing mean-reversion (fade 2-ATR Daily extension) | XAUUSD | H4/D | 1% risk, 1.5R, multi-day, no tuning | **KILL.** grPF 0.94, netPF 0.85, Sharpe -0.52, MaxDD 26.9%, 274 trades | medR 10$/oz → cost_R 6.1%. NEGATIVE even gross (-0.038R): fading gold extensions loses because trends continue (momentum). Loses to B&H. Guard PASS. |
| 2026-07-18 | **CONCLUSION (swing vs intraday)** | XAUUSD | H4 vs M15 | — | Cost hypothesis confirmed; no edge found | Widening stops cut cost_R from ~30%→4-6% (order-of-mag improvement) — **costs are NOT the binding constraint at swing scale.** But gross edges are ~0 (A +0.007, B +0.031, C -0.038 R/trade). Simple price-only gold timing (trend pullback / breakout-retest / extension-fade) has NO exploitable edge at ANY timeframe; intraday costs merely masked that the edges were near-zero anyway. **None beats buy-and-hold gold (SR 1.19).** |
| 2026-07-18 | **SYSTEMATIC SWEEP (5 families × 5 TF)** | XAUUSD | M5/M15/M30/H1/H4 | 5 families (trend/breakout/meanrev/momentum/macross) × 3 stated variants each × 5 TF = **75 configs**, 1% risk, fixed-R, no tuning, real spread + $0.07/oz comm + news slip | **NONE SURVIVE.** 0/75 clear DSR haircut; 5/75 have +net Sharpe; 8/75 net PF>1; 3/75 OOS-holds; guard PASS 75/75 | True trial count N=75. Best raw = macross H4 v2 (SR +0.23, netPF 1.20, MDD 19.9%) but DSR≈0.000 (within noise of 75 trials) and FTMO pass 0.0%. All 15 M5 configs SR −4 to −15 (costs crush ~2$/oz stops). Edge concentrates weakly at H4 (widest stops) but is statistically indistinguishable from zero after multiple-testing correction. Results: results/sweep_progress.csv + leaderboard.csv. **Confirms & generalizes the prior swing/intraday conclusion across the full family×TF grid.** |
| 2026-07-20 | **HTF-trend-gated swing breakout (CONTINUATION)** | XAUUSD | M5/M15/M30 gated by H1 | H1 trend = HH&HL / LH&LL on 3-bar pivots (causal, ffill-mapped); zone = most recent confirmed trading-TF swing hi/lo; entry on bar CLOSE beyond zone in trend direction; stop = opposite extreme of breakout bar − 0.1×ATR(14) (=1R); min R 0.25×ATR; max hold 48 bars; 1 position; 1% risk. Grid **3 TF × 2 R (1.5/2.0) × 2 pivot N (3/5) = 12 configs**. No tuning. | **KILL.** 0/12 positive net Sharpe, 0/12 netPF>1, 0/12 clear DSR, 0/12 OOS-holds, guard PASS 12/12. Best = M30 N=3 R=2.0: netPF 0.77, SR −1.97, DSR 0.000, MDD 94.2%, 1370 trades, FTMO P1 5.3% | Cumulative trial count **N=87** (75 prior sweep + 12 here); DSR deflated against all 87 Sharpes, not this batch alone. **Gross edge is real but trivial and cost-dominated:** M30 +0.037R/trade (grPF 1.07) vs cost 0.215R/trade → cost = 21% of R. M15 +0.019R vs 32% cost. **M5 is negative even GROSS** (−0.025R, grPF 0.96) at 60% cost. Stops are tiny (medR 1.1–3.1 $/oz) because the stop is the breakout bar's own range — commission alone ($0.07/oz) is 2–6% of R. No DSR survivor → no separate OOS re-test needed; best config's OOS is negative anyway (IS PF 0.72 / OOS PF 0.92, OOS SR −0.85). Loses to B&H gold (SR 1.19) by −3.16. **Confirms the breakout-retest family kill generalizes to trend-gated continuation:** the HTF gate does not rescue it. Results: results/htf_breakout.csv + htf_breakout_scored.csv |
| 2026-07-10 | ICT SMC Full Model v2 (FTMO) | XAUUSD | — | v2 Pine fixes: barstate.isconfirmed, pc_sym=XAUUSD, FVG sweep gate | Not yet backtested | Pine compile fixes applied; pending live TradingView test and systematic backtest with spot data |
| 2026-07-21 | **US INDEX CFD SWEEP (5 families × 5 TF × 2 instruments)** | NAS100 + US30 | M5/M15/M30/H1/H4 | 2 inst × 5 TF × 5 families × 3 stated variants = **150 configs**, 1% risk, fixed-R, no tuning, REAL per-bar Dukascopy bid/ask spread + 0.35bps comm (round-turn) + 0.15/0.50bps per-side slippage. Data: 2.21M (NAS100) + 2.09M (US30) M1 bars, 2018-01-03→2025-12-31 UTC, verified full-span before run. | **NONE SURVIVE.** 0/150 clear DSR; 21/150 +net Sharpe; 30/150 netPF>1; **90/150 gross PF>1**; 21/150 OOS-holds; guard PASS 150/150. Best = NAS100 H4 macross v0: grPF 1.52, netPF 1.47, SR +0.67, MDD 13.7%, 177 trades, cost_R 1.9%, **FTMO 0.0%**, OOS holds YES | Cumulative trial count **N=237** (75 sweep + 12 HTF + 150 here); DSR deflated against all 237. **COST HYPOTHESIS CONFIRMED — costs roughly HALVED vs gold at every TF:** M5 27.8% vs gold ~60%, M15 14.4% vs ~32%, M30 8.9% vs ~21%, H4 2.5-2.7% vs ~4-6%. **But cheap costs did NOT create an edge.** At M5-M30 gross edge is still ~0 (grPF 1.00-1.01) — costs were never the binding constraint, confirming the gold conclusion. **At H4 there IS a real gross edge, the best in this project: grPF 1.12 (NAS100) / 1.16 (US30), +0.064/+0.082 R/trade, surviving costs to netPF 1.07/1.11, 21/30 H4 configs netPF>1.** Edge is monotonic in TF and concentrated in macross/trend — consistent with genuine index trend persistence, not noise. **Yet it still fails every deployment gate: FTMO P1 pass 0.0% (holding period too long / DD timing), and NAS100 best LOSES to B&H (SR +0.67 vs +0.84).** US30 best "beats" B&H by +0.01 Sharpe = noise, NOT a win. **DSR CAVEAT (methodological, reported honestly):** DSR = 0.000 for ALL 150 configs and is structurally saturated, not discriminating — the 237-trial Sharpe universe has σ=3.38 (min −14.59) because the M5 configs are catastrophic, so E[max Sharpe | null] ≈ σ√(2 ln N) ≈ 11, which no realistic config can exceed. Including known-doomed M5 configs in the deflation universe makes DSR uninformative here. **The verdict does not rest on DSR:** FTMO 0.0% and the B&H comparison kill independently. Results: results/sweep_indices.csv + sweep_indices_scored.csv + leaderboard_indices.csv |
| 2026-07-22 | **DSR GATE REPAIR (methodology fix, no new backtest)** | — | — | New module `research/dsr.py`; driver `recompute_dsr.py` → `results/dsr_recomputed.csv` | **The old DSR was broken TWO ways, and every DSR figure in this log above is unreliable.** (1) **Units bug:** numerator `(sr_best − E[maxSR])` in ANNUALISED units, denominator the SE of the PER-PERIOD Sharpe → SE 0.0313 where it should be 0.3376, **10.8× too small**; z inflated ~11-16×, so DSR collapsed to a step function at E[maxSR] (exactly 0.0000 or 1.0000 — the pattern seen in every study here). Also restored Mertens (2002) variance (old code wrote `1+(1−…)`=`2−…`, dropping the 0.5·SR² term, adding a spurious +1). (2) **Contaminated pool:** E[maxSR] scales with pool σ; the 237-trial pool held M5 configs at SR −14.6 → σ=3.39 → **E[maxSR]=+6.78**, a bar nothing can clear. Haircut was set by how badly the WORST configs failed. | **Fixed pools (stated):** structural (a priori: TF H1+H4 × families trend/macross/momentum) N=36, E[maxSR]=+0.921 — the headline gate; Sharpe-floor ≥−1 N=92, E[maxSR]=+0.797 — sensitivity ONLY, since filtering on realised Sharpe is selection-on-outcome that shrinks σ and makes passing EASIER. **Recomputed prior index top-15: 0/15 clear DSR>0.95.** Best (NAS100 H4 macross v0, SR +0.67): z=(0.67−0.92)/0.338=−0.75 → **DSR 0.227** (floor pool 0.352). Gate now discriminates (values spread 0.02–0.35) instead of saturating. **The H4 macross edge is real in-sample but NOT distinguishable from the best-of-36 expected by chance.** metrics.py left untouched so historical crypto verdicts aren't silently restated. |
| 2026-07-22 | **DIVERSIFIED INDEX TREND BASKET (6 instruments × H4/H8/D1 × trend+macross)** | NAS100, US30, SPX500, GER40, UK100, JP225 | H4 / H8 / D1 | **108 single-name configs + 18 equal-risk baskets.** 6 inst × 3 TF × 2 families × 3 stated variants, no tuning. ONE config applied to all 6 for each basket (no per-instrument selection = no cherry-picking). Data: H1 bid+ask Dukascopy 2018-01→2025-12, IDs verified from enum + price probe (DAX 17.7k, FTSE 7.65k, N225 40.1k, SPX 5.13k). Signals on H4/H8/D1 (label-right), trades resolved on the FINER H1 base — stricter than the prior resolve-on-signal-TF run. Real spread + 0.35bps comm + 0.15/0.50bps slip, 1% risk. | **KILL. 0/108 single-name and 0/18 baskets clear DSR>0.95 + OOS-holds.** Guard PASS 108/108. Best basket = H4 macross v2: SR **+0.80**, netPF 1.20, maxDD **7.8%**, CAGR 4.0%, vol 4.1%, DSR **0.446**, OOS holds YES (IS +0.70 / OOS +0.95). Best single-name = NAS100 D1 trend v0 SR +1.07, DSR 0.208. | Cumulative trials **N=345** (237 prior + 108). **Diversification mechanically WORKS:** mean member pairwise corr **0.02–0.14** (genuinely near-independent); 11/18 baskets beat their AVERAGE member (mean uplift +0.078 SR; best cell +0.39: basket 0.80 vs avg member 0.41); basket maxDD 7.8% vs ~32-40% for B&H. **0/18 beat their BEST member — but that comparison is ex-post cherry-picking and is NOT the valid test.** **Family split is the real finding: macross baskets mean SR +0.438, trend baskets mean SR −0.072** — the two "trend" families behave oppositely; trend-continuation fails entirely. **vs benchmarks:** best basket BEATS EW B&H basket (+0.80 vs +0.63, gap +0.17) with 4× lower drawdown, but LOSES to best single B&H (NAS100 +0.84, gap −0.04). Vol-matching to B&H needs **3.7× leverage** → ~14.6% CAGR at ~28.9% maxDD, and financing cost + gap risk at 3.7× are NOT modelled — the Sharpe win does not survive as a return win without leverage this backtest can't validate. **CAVEAT: OOS Sharpe > IS Sharpe on most baskets (e.g. H8 macross v2 IS +0.34 / OOS +1.34) — 2023-2025 was a strong index trend regime, so OOS strength is REGIME, not robustness.** DSR basket pool N=18 is correlated (shared instruments/families) so effective N<18 → E[maxSR] overstated → DSR conservative. FTMO best 1.1% (bar 30%) — not viable, as expected for swing. Results: results/basket_configs_scored.csv + basket_results.csv |
| 2026-08-11 | **PRE-2018 OUT-OF-REGIME TEST of the index-trend basket (STATE_OF_PLAY §5 step 3)** | NAS100, US30, SPX500, UK100, JP225 | H4 / H8 / D1 | **90 single-name configs + 18 baskets per window, run on TWO windows.** Strategy code, families, variants, cost model and engine byte-identical to `run_basket_trend.py` (same imports) — **zero re-tuning**. Only the data window changes. Windows: **pre2018** 2013-09-30→2017-12-29 (split 2016-01-01, ~53/47) and matched **new5** 2018-01-02→2025-12-31 (split 2022-01-01, 50/50). BOTH on the same 5 indices so the comparison isolates REGIME, not membership. Real per-bar bid/ask spread + 0.35bps comm + 0.15/0.50bps slip, 1% risk. Data verified by `scripts/verify_pre2018.py` (hard gate): span, per-year bar floors, price bands, spread sign, coverage gaps — 5/5 PASS. Guard PASS 90/90 both windows. | **KILL — the lead does NOT survive out of regime.** Headline H4 macross v2 basket, in-regime → out-of-regime: net Sharpe **+1.042 → −0.283** (sign flip), netPF **1.292 → 0.935**, **gross PF 1.363 → 1.006**, maxDD **5.7% → 10.8%** (worse while losing), CAGR +5.66% → −1.32%, OOS-holds **YES → NO** (IS −0.223 / OOS −0.371), DSR **0.690 → 0.143**. Across all 18 baskets: positive-Sharpe **12/18 → 2/18**, netPF>1 **12/18 → 2/18**, OOS-holds **7/18 → 1/18**, DSR>0.95 **0/18 → 0/18**. Best pre-2018 basket is D1 macross v0 at SR **+0.097** — indistinguishable from zero. | **DATA AVAILABILITY (probed empirically, `scripts/probe_earliest.mjs` + `probe_h1_sides.mjs` + `probe_refine.mjs`, retries off): 2008 IS UNREACHABLE.** `instrumentMetaData` H1 starts: NAS100/SPX500/UK100/JP225 2011-09-18, US30/GER40 2013-09-30 — but the metadata is optimistic and the ASK archive starts later than the bid. Verified spread-usable (real bid AND real ask) starts: SPX500 2012-02, NAS100 2012-04, JP225 patchy through 2012 (bid without ask at 2012-02, ask without bid at 2012-04), UK100 **2013-09**, US30 2013-09-30, **GER40 2015** (ask genuinely absent 2013-2014 — re-probed fresh, NO DATA not a cached fault). So **2013-09-30 is the real floor for a 5-index spread-costed basket**, and GER40 cannot be included at all. The 2008 GFC and 2011 selloff CANNOT be tested with Dukascopy index CFDs, full stop — this test covers 2014 oil crash, 2015 China Black Monday, 2016 Brexit only. **WHY IT FAILS — the edge is gone BEFORE costs.** cost_R rose 3.56%→5.05% of 1R (wider pre-2018 spreads), but that ~1.5% cannot explain a 1.33-Sharpe swing; **gross PF 1.006 means the raw signal edge is literally nil**. Mechanism: 2013-2017 was a *smooth grinding* bull — EW B&H Sharpe **+0.96 at only 17.1% maxDD** vs 2018-25's **+0.66 at 30.7% maxDD** (near-identical CAGR 11.8% vs 11.2%). Macross harvests large persistent directional moves; 2018-25 supplied three (2020 crash+recovery, 2022 bear, 2023-25 AI rally) and 2013-17 supplied none, so the crossover just whipsawed. **The basket also LOSES to buy-and-hold pre-2018 by −1.25 Sharpe (−0.28 vs +0.96)**, having beaten it by +0.39 in regime. **Family finding (§3) partially survives: macross still beats trend-continuation out of regime (+0.369 gap, mean SR −0.233 vs −0.602) — but both are NEGATIVE, so it is a relative ordering, not an edge.** DSR pools STATED (`research/dsr.py`): single-name structural pool N=90 (H4/H8/D1 × trend+macross, this run's own a priori cells), E[maxSR] +1.104 (pre2018) / +1.376 (new5); basket pool N=18, E[maxSR] +0.228 (pre2018) / +0.896 (new5). Note the pre-2018 basket bar is LOW (+0.228, because every basket is bad) and the lead STILL only scores 0.143. Cumulative trials **N=435** (345 prior + 90 pre-2018); the 90 `new5` configs are a re-scoring of the already-counted 2018-25 grid (5 of 6 instruments, moved split) and are NOT double-counted. **CAVEAT reported, not hidden:** pre-2018 H1 coverage is ~30% sparser (3,566-4,286 bars/yr vs 5,739-5,822 for SPX500/UK100/JP225 in 2018-25) with 154-319 lost days per member and gaps up to 93 days; JP225's archive stops 2017-11-30. Per-year TRADE rates are nonetheless comparable (133/yr pre-2018 vs 146/yr in regime), so sparsity thinned the sample without gutting it — it makes the pre-2018 Sharpe noisier, but cannot manufacture a sign flip plus a gross PF collapse to 1.006. Results: results/basket_results_pre2018.csv, basket_configs_scored_pre2018.csv, basket_results_new5.csv, basket_configs_scored_new5.csv, pipeline_pre2018.log. Reproduce: `scripts/run_pre2018_compare.cmd` (verify gate → both runs, detached); compare with `scripts/compare_pre2018.py`. |
| 2026-08-19 | **15-MINUTE "SNEAKY PIVOT" (Strategy 2 of the 4-strategy brief)** | NAS100, US30, XAUUSD | M15 signal, M1 resolution | **24 configs**: 3 instruments x 2 targets (range-opposite / swing-opposite) x 2 stops (sneaky-candle extreme / low-of-day) x 2 trigger windows (C3-only = literal reading / rest-of-session = spirit). Every axis is a fork the brief itself leaves open; **no numeric parameter is fitted**. Session = RTH 09:30-16:00 America/New_York; Range High/Low = prior RTH session extremes; Swing High/Low = nearest 5-bar daily pivot beyond the range, usable only once CONFIRMED (pivot at day i available from day i+N, gated to <= d-1). Setup: C1 trades at/through the zone line AND is directional into it; C2 reverses (green at support / red at resistance); entry = stop order at C2's extreme; stop = C2 extreme or LOD; target = opposite range or swing line; force-flat at the cash close. Data: 2.21M (NAS100) + 2.09M (US30) + 1.83M (XAUUSD) M1 bars, 2018-01 -> 2025-12 UTC. Real per-bar bid/ask spread + 0.35bps comm + 0.15/0.50bps slip (indices); real spread + $0.07/oz + $0.03/$0.10 per side (gold). 1% risk/trade. | **NO SURVIVOR, BUT NOT A KILL EITHER - the first family in this project where the GROSS edge is unanimous.** 24/24 gross PF > 1 (range 1.170-1.548). 20/24 netPF>1, 20/24 +net Sharpe, 15/24 OOS-holds, guard PASS 24/24, **0/24 clear DSR > 0.95**. Best = NAS100 swing/sneaky/session: grPF **1.536**, netPF **1.384**, net Sharpe **+0.53**, maxDD 14.7%, 189 trades, win 40.7%, IS PF 1.26 / OOS PF 1.60 (OOS SR +2.15, 76 trades). | Cumulative trials **N=459** (435 prior + 24). **DSR pool STATED, and the convention is corrected here:** headline = STRUCTURAL pool = this batch's own 24 a priori cells, E[maxSR] **+0.590** (mu +0.202, sd 0.196) -> best config **DSR 0.421**. The project-cumulative 459-Sharpe pool is printed for CONTRAST ONLY (E[maxSR] +7.08, sd 2.82) because it is the exact sigma-contamination `research/dsr.py` BUG 2 documents; earlier runners used it as the gate, which is why they all read 0.000. **What DSR does and does not say here:** it tests the MAXIMUM against best-of-24 noise and the max fails (+0.53 vs a +0.59 bar). It says nothing about the far more striking fact that **every single cell is positive gross across three different instruments** - sign-consistency is not a best-of-N artefact, and the family mean SR is +0.202. **Costs are NOT binding on indices** (cost_R 5.7-8.2% of 1R) but ARE on gold (14.4-17.3%), which is exactly why all four net-negative configs are XAUUSD session variants - a fifth independent confirmation of the §1 structural finding. **Honest caveat on the "swing target" cells:** they post the best gross R (+0.29/trade on NAS100) but hit their stated target only **3 times in 189 trades** - those configs are in practice "hold to the cash close with a stop", not the strategy as written. The extra R comes from letting winners run to the bell, not from the swing line. **REGIME CONCENTRATION - the same signature that killed the last lead:** NAS100 best config earns +27.2R in 2025 alone out of ~+42R total, and is negative in 2020 (-5.9R) and 2024 (-7.1R); US30 best is -14.8R in 2023. **Loses to buy-and-hold on all three instruments** (NAS100 +0.53 vs +0.84; US30 +0.32 vs +0.55; XAUUSD +0.21 vs +1.19) though at 2-3x lower drawdown. **VERDICT: parked pending the out-of-regime test, per §7 rule 3 - test 2013-2017 BEFORE believing anything.** The repo has no pre-2018 M1, so `scripts/download_pre2018_m1.mjs` was written and launched to pull 2013-09-30 -> 2018 M1 bid+ask for NAS100 and US30. **Data fact probed 2026-08-19 (not assumed):** pre-2018 index M1 quotes cover ONLY 13:30-20:00 UTC (the US cash session, ~371 bars/day) in 2013, widening to 06:00-20:00 UTC (~840/day) by 2016; bid and ask merge 100% on timestamp. That is sufficient for an RTH-anchored strategy and retroactively justifies the RTH session choice - the same definition is testable in both regimes. Dukascopy rate-limits M1 hard (HTTP 429 at batch 40/300ms), so the downloader runs at batch 8/1500ms with minute-scale 429 backoff and an on-disk cache. Results: results/sneaky_pivot.csv, sneaky_pivot_scored.csv, sneaky_pivot_trades.csv, sneaky_pivot_run.log. Reproduce: `py -3.14 run_sneaky_pivot.py` (or `--analyze` to re-score without reloading 300 MB). |
| 2026-08-21 | **PRE-2018 OUT-OF-REGIME TEST of the 15-minute Sneaky Pivot (STATE_OF_PLAY 9.3)** | NAS100, US30 | M15 signal, M1 resolution | **16 configs** (2 inst x 2 targets x 2 stops x 2 triggers), run by `run_sneaky_pivot_pre2018.py`, which imports `run_sneaky_pivot` as a MODULE and rebinds only 4 names (data files, OOS split 2016-01-01, output paths, banner label) before calling its `main()`. Strategy, cost model, engine, scorer and gates are the SAME OBJECTS as the 2018-2025 run -- zero re-tuning, zero re-implementation. XAUUSD drops (no pre-2018 gold M1), so 16 cells not 24. Data: NAS100 443,449 M1 bars / 1,030 sessions, US30 467,543 / 1,082, both 2013-09-30 -> 2017-12-29, RTH 13:00-21:00 UTC, negSpread=0, per-year 102k-112k bars vs a 55k gate floor. | **THE GROSS EDGE SURVIVES -- the first time anything in this project has done that.** gross PF > 1: **16/16 -> 14/16**, mean **1.321 -> 1.155**, best cell 1.536 -> 1.363, and one cell IMPROVED (NAS100 swing/sneaky/c3, 1.529 -> **1.547**). Compare the index basket's kill: 1.363 -> **1.006**, edge annihilated before costs. **But it is NOT a survivor:** net PF>1 **16/16 -> 3/16**, positive net Sharpe **16/16 -> 3/16**, OOS holds **0/16**, DSR > 0.95 **0/16** (best 0.53 vs E[maxSR] +0.225 on the 16-cell structural pool), guard PASS 16/16. | Cumulative trials **N=475** (459 + 16). **Costs: the caveat written from partial data was wrong in its mechanism and has been corrected in the runner.** Full-window NAS100 spread is 2.39 bps vs 2.31 in regime -- essentially EQUAL, not 2.5x wider (2013-14 was wide, 2015-17 tightened; the partial window caught only the wide half). Yet cost_R still runs **15-17% of 1R vs 5.8-6.8%**. It is a STOP-DISTANCE story, not a spread story: 1R is the sneaky candle's own range and 2013-2017 was a low-vol grind, so stops are ~2.5x tighter and an unchanged spread eats 2.5x more of them. Section 1's vice reached from the other side. **REGIME CONCENTRATION REPEATS, and this is the strongest mark against.** In regime the NAS100 best cell earned +27.2R in 2025 out of ~+42R; out of regime the best cell earns **+29.2R in 2017 out of +14.8R net total**, and is NEGATIVE in 2014 (-3.7), 2015 (-9.1) and 2016 (-3.3). US30's best is +10.1R in 2016 against -9.1 in 2015 and -7.0 in 2017, summing NEGATIVE (-4.2R). One year carries each window. **Loses to buy-and-hold in both regimes and by more out of regime:** best +0.26 vs NAS100 B&H **+1.21**, US30 B&H +1.05 (in regime it was +0.53 vs +0.84). **Reading: the setup finds a real, repeatable gross inefficiency -- 14/16 cells positive gross across two instruments and two disjoint 4-8 year windows is not a best-of-N artefact -- but the effect is too small to clear its own transaction costs at 15-17% cost_R, its P&L is one-year-concentrated in both windows, and it is dominated by simply holding the index. Not a lead. Not the basket's clean kill either.** Data note: 71 confirmed single-side archive holes (paced re-fetch recovered 0/71, so they are real), costing NAS100 ~50 sessions; per-year bar counts still clear the gate by ~2x. Results: results/sneaky_pivot_pre2018.csv, sneaky_pivot_scored_pre2018.csv, sneaky_pivot_trades_pre2018.csv, sneaky_pivot_pre2018_run.log. Reproduce: `py -3.14 run_sneaky_pivot_pre2018.py`. |
| 2026-08-26 | **OPENING RANGE BREAKOUT at the US CASH OPEN (09:30 ET) — in regime AND out of regime** | NAS100, US30 | M1, RTH 09:30-16:00 ET | **12 configs per window, run on TWO windows**: 2 instruments x 2 opening ranges (15 / 30 min) x 3 targets (1R / 2R / hold-to-cash-close). Stop = the OPPOSITE side of the opening range, so **1R IS the OR range** — the brief's "or a stated fraction of it" was NOT used, so no fraction is a free parameter. Entry = a resting stop order at the OR extreme, armed only from 09:30+N; first break of the day wins; ONE position per day per instrument; force-flat at the 16:00 ET cash close; no overnight holds. **No numeric parameter is fitted anywhere.** Windows: **in regime** 2018-01-03 -> 2025-12-31 (split 2023-01-01) and **out of regime** 2013-09-30 -> 2017-12-29 (split 2016-01-01). `run_orb_pre2018.py` contains NO strategy, cost or scoring code — it imports `run_orb` and rebinds five names (files, split, output paths, prior-trial count, banner), so both windows execute the SAME objects. **Same 12 cells on both sides** (both indices have M1 in both windows), so the comparison is exactly like-for-like — unlike the Sneaky Pivot test, which lost gold. Costs: REAL per-bar Dukascopy bid/ask spread (round-turn) + 0.35 bps commission + **1.00 bps per-side slippage for entries 09:30-10:30 ET** / 0.15 bps after, 1% risk/trade. | **KILL — and the cleanest of the three out-of-regime kills in this project.** In regime the raw effect is real and unanimous: **gross PF > 1 in 12/12** (1.100-1.174, mean 1.141), guard 12/12 PASS. It then fails EVERY other gate: net PF > 1 **5/12**, positive net Sharpe **5/12**, OOS holds **1/12**, DSR > 0.95 **0/12** (best 0.381), not-year-concentrated **0/12**, beats B&H **0/12**, **SURVIVORS 0/12**. Out of regime the edge does not merely shrink, it **inverts**: gross PF > 1 **12/12 -> 2/12**, mean **1.141 -> 0.960** (i.e. mean gross PF is BELOW 1 — the average cell loses money before costs), mean Sharpe **-0.153 -> -2.316**, net PF > 1 **5/12 -> 0/12**. Cells positive-gross in BOTH windows: **2/12**. Cells net-profitable in both windows: **0/12**. Best in-regime cell (NAS100 OR30 2R): gross PF **1.174 -> 0.965**, net PF **1.039 -> 0.730**, Sharpe **+0.23 -> -2.13**. | Cumulative trials **N=499** (475 prior + 12 in regime + 12 out of regime). **DST — the thing that silently breaks this strategy, handled and VERIFIED.** 09:30 ET is 13:30 UTC under EDT and 14:30 UTC under EST; a fixed offset would build the "opening range" from 08:30-08:45 ET pre-market all winter and 10:30-10:45 ET all summer. Every bar is converted with `tz_convert('America/New_York')` (full IANA history per timestamp), and `scripts/verify_orb_sessions.py` is a HARD GATE that proves it from the data, not from an assumption: across all four files the 09:30 ET bar occurs at **13:30 UTC and 14:30 UTC ONLY** (NAS100 2018-25: 1,075 EDT / 604 EST sessions), the observed offset flips land on the first session after the 2nd Sunday in March and the 1st Sunday in November for all 8 years, the opening bar is present on 100%/99.9% of in-regime and 98.5%/98.9% of pre-2018 sessions, the OR15 window has >=13 of 15 bars on 98-100%, and 15:59 ET is reached on 99%/96%. The gate exits 1 and blocks the sweep. **LOOK-AHEAD.** Two independent checks, 12/12 PASS both windows: the statistical guard (`guard_look_ahead`, threshold 0.5) and an ORB-specific assertion that no candidate is armed before its OR is complete — measured earliest entry minute equals **exactly** 09:45 ET (OR15) and 10:00 ET (OR30) in every cell. Resolution runs `strictly_after=False` **on purpose**, so the breakout minute's own adverse excursion is charged (stop-first on same-bar ties); skipping that bar would hand the strategy a free minute of the most volatile tape of the day. **COSTS.** cost_R **5.7-9.9% of 1R in regime**, **11.9-17.5% out of regime** — and this is a STOP-DISTANCE story, the same one section 1 and the Sneaky Pivot found from the other side: median 1R falls from 45 bps to 31 bps of price in the low-vol 2013-2017 grind, so an unchanged spread eats far more of it. Costs are NOT what kills this in regime (gross R/trade +0.040 to +0.091 vs cost 0.057-0.099) — **out of regime the gross number itself is negative.** The 1.00 bps opening-hour slippage is a judgement call, so a 0.50 bps sensitivity (the repo's existing news figure) is reported on the SAME trades — a re-scoring, not new trials: it lifts net PF by only +0.035 to +0.066, moving 2 more in-regime cells above 1.00 and changing no verdict. **SINGLE-YEAR CONCENTRATION — 0/12 pass, the third appearance of the signature that killed the last two candidates.** The best cell earns +21.3R of its +28.9R total in 2023 (**74%**) and is NEGATIVE in 2018, 2019, 2020 and 2025. Three other net-positive NAS100 cells have a single year worth **154%, 180% and 193%** of their total (positive year larger than the whole P&L). All six US30 cells sum NEGATIVE over 2018-2025. **vs BUY-AND-HOLD: loses 4/4** — NAS100 +0.23 vs **+0.84** and US30 -0.20 vs **+0.55** in regime; -1.37 vs **+1.21** and -1.70 vs **+1.05** out of regime — and it loses on drawdown too (33.5-61.0% vs 35.7%/37.0%). **DSR pool STATED:** headline = STRUCTURAL = this batch's own 12 a priori cells, E[max SR] **+0.341** (mu -0.153, sd 0.297) -> best cell **DSR 0.381**. The project-cumulative 487-Sharpe pool is printed for CONTRAST ONLY (E[max SR] +7.00, sd 2.75) — the exact sigma-contamination `research/dsr.py` BUG 2 documents. **HOW THIS COMPARES TO THE OTHER TWO OUT-OF-REGIME TESTS — ORB is the worst of the three.** Index basket 1.363 -> 1.006 (edge annihilated); Sneaky Pivot 1.321 -> 1.155 with 14/16 cells still gross-positive (edge survived, could not pay for itself); **ORB 1.141 -> 0.960 with 2/12 gross-positive — it does not merely lose its edge, it goes gross-negative.** **ON THE EXTERNAL EVIDENCE.** The documented NQ ORB survivor that motivated this test is NOT reproduced. What our data does show is that the in-regime gross signature is exactly what such a study would report — 12/12 gross-positive cells on 2018-2025, net-positive on NAS100 — and that it does not exist before 2018. That is consistent with (though not proof of) the published result being a 2018-2025 artefact, the same failure mode as this project's own last two candidates. A different ORB is still possible (a trend/VWAP filter, volatility-scaled sizing, a different stop fraction, NQ futures costs rather than index-CFD costs) — but each of those is a DIFFERENT strategy with its own trials, and the plain, evidence-backed form tested here does not survive. **SPX500 was requested and is ABSENT for a data reason, not a choice:** the repo holds SPX500 at H1 only, and a 15-minute opening range cannot be built from hourly bars. NAS100 and US30 are the two indices with M1 in BOTH windows, which is what makes the out-of-regime test possible at all. **Engine change, audited:** `simulate_trades` gained an OPTIONAL `slip_bps_fn` so slippage could be defined in ET (the existing NEWS_HOURS_UTC windows are fixed in UTC and END at exactly 09:30 ET under EST, which would have charged every winter ORB entry NORMAL slippage in the most volatile minute of its day). Default `None` reproduces the old branch exactly — verified by a synthetic regression exercising both the news and normal paths: default == explicit-None and cost_R == the original formula to 1e-15. No prior result moves. Results: results/orb.csv, orb_scored.csv, orb_trades.csv, orb_pre2018.csv, orb_scored_pre2018.csv, orb_trades_pre2018.csv, orb_run.log. Reproduce: `scripts/run_orb.cmd` (verify gate -> in regime -> out of regime, detached, ~80 s). |
| 2026-08-27 | **5-family sweep at M1 - THE MISSING TIMEFRAME ROW** | XAUUSD, NAS100, US30 | **M1** | Same 5 families x 3 stated variants imported UNCHANGED from `strategies/sweep_families.py` (the same objects run_sweep.py and run_sweep_indices.py used); every parameter is in BARS, so at M1 ATR14 = 14 min, EMA200 = 200 min, max-hold H = 12-96 MINUTES; strictly_after=True, de_overlap, 1% risk/trade, real per-bar spread; XAUUSD on the legacy $/oz cost model and the indices on 0.35 bps commission + 0.15/0.50 bps per-side slippage - i.e. each instrument keeps the cost model it already used at M5-H4, which is what makes the new row comparable to the five above it | **KILL, and a two-part finding that must not be merged.** 45 in-regime cells: look-ahead guard **45/45 PASS**, gross PF > 1 **24/45** (mean **1.0049**, median 1.0024, range 0.946-1.071), net PF > 1 **0/45**, net Sharpe > 0 **0/45**, DSR > 0.95 **0/45**, OOS holds **0/45**, top-year <= 60% **0/45**, beats B&H **0/45**, **SURVIVORS 0/45**. Best net Sharpe NAS100 macross v1 **-13.72** (gross PF 1.0278, net PF 0.4398, cost_R 60.5%). Out of regime (2013-09-30 -> 2017-12-29, 30 cells against an RTH-MATCHED in-regime control): mean gross PF **0.996 -> 0.988**, net PF > 1 **0/30 -> 0/30**, cells positive-gross in BOTH windows **3/30** (all NAS100 macross), net-profitable in both **0/30**. | Cumulative trials **N=574** (499 prior + 45 in regime + 30 out of regime; the 30 RTH-matched control cells are a RE-SCORING of an already-counted grid on a data subset and are NOT double-counted, following the section 6 precedent). **FINDING (1) - A GROSS EDGE AT M1 EXISTS, IS STATISTICALLY REAL, AND IS ~3% OF ITS OWN COST.** This is the useful result and it is NOT 'no edge'. `scripts/m1_gross_significance.py` tests gross R per trade against zero on the best cell of each family, with a daily-block t so intraday clustering cannot inflate it: XAUUSD meanrev **+0.0398 R/trade, t=+6.89 per-trade / +9.61 daily, p=5.5e-12**; NAS100 macross +0.0288, t=+3.66/+6.48, p=2.5e-4; NAS100 trend +0.0178, t=+3.12/+3.78, p=1.8e-3; NAS100 momentum +0.0107, t=+1.65/+2.83 (marginal); NAS100 breakout -0.0040, t=-0.76/-0.03 (zero). Expressed against the cost it must pay, that same edge is **2.96%, 3.41%, 3.49%, 1.68% and -0.50%**. **The family ordering is systematic, not noise: meanrev 8/9 cells gross-positive (+0.0169 R/trade), macross 6/9, trend 6/9, momentum 4/9, breakout 0/9 (-0.0154 R/trade, negative on every one of 9 cells across all three instruments).** The M1 tape MEAN-REVERTS and BREAKOUTS FAIL - a clean structural anti-finding with no counterpart at M5-H4. **FINDING (2) - THE COST GRADIENT IS CONFIRMED ON LIKE-FOR-LIKE DATA AND FLATTENS COMPLETELY ONCE THE OVERNIGHT TAPE IS REMOVED. The second half was NOT predicted.** Prediction stated before the run: cost per trade is fixed while 1R is an ATR-scaled stop and ATR ~ sqrt(bar duration), so M5 -> M1 should multiply cost_R by sqrt(5) = 2.24x. On the 23-hour session-agnostic data the M5-H4 rows used, measured M1 cost_R is **XAUUSD 88.5%, NAS100 71.5%, US30 68.6%** = **2.71x / 2.57x / 2.64x** of M5 - the gradient holds and is **steeper than predicted** (+15% to +21% above the sqrt-5 forecast), and **45/45 cells sit above the 20% cost_R band that killed M5**. BUT restrict M1 to the US cash session [13:00,21:00) UTC - the liquid tape any real M1 trader would use, and the window the pre-2018 archive forces - and cost_R collapses to **NAS100 26.4%, US30 26.5%**, i.e. **0.95x and 1.02x of M5: the gradient is FLAT across the M5 -> M1 step.** Two effects compound: the cash session's median spread is 2.2x tighter (1.03 vs 2.31 bps on NAS100) AND its 1R is 1.9x larger (11.2 vs 6.0 bps) because cash-session minutes are more volatile. **So the honest statement is that M1 is not intrinsically more cost-punished than M5 - trading M1 across the 23-hour tape is.** It rescues nothing: at 26% cost_R net PF is still 0/30, because a 3%-of-cost gross edge cannot pay a cost of any size. **The cost figures quoted in the brief (M5 60%, M15 32%, M30 21%) come from `htf_breakout.csv` (stop = the breakout bar's own range), NOT from the 5-family sweep this row extends** - verified: HTF breakout M5 60.3% / M15 32.5% / M30 21.9%; family sweep M5 32.7% / M15 17.3% / M30 11.4% / H1 7.6% / H4 3.7%. Both are real; the family-sweep gradient is the correct baseline here and is the one used above. **SHARPE ANNUALISATION - the trap, verified rather than asserted.** Headline Sharpe is computed on CALENDAR-DAILY aggregated returns annualised at **252**, because the annualisation factor is a property of the RETURN SERIES, not of the signal timeframe (an M1 system and a D1 system both emit one return per trading day). Measured M1 bars/year is **228,550 (XAUUSD) / 276,723 (NAS100) / 261,531 (US30)** - note this is NOT `metrics.py::BARS_PER_YEAR['1m'] = 525,600`, which assumes a 24/7 year and overstates the real count by 1.9-2.3x. Annualising per-BAR returns at the measured count instead would have inflated Sharpe by **30.1x / 33.1x / 32.2x** (e.g. -24.05 -> -724.34). `scripts/verify_m1.py` is a HARD GATE that proves the identity SR_perbar/SR_daily == sqrt(bars_per_year/252) arithmetically before any strategy number is read, and also proves that `resample_mid(m1,'1min')` is EXACTLY the native bar relabelled to its close time, so the M1 execution frame uses the identical convention as M5-H4. **LEVERAGE / RUIN, reported not buried.** At the repo's 1% risk/trade convention M1 fires ~25 trades/day, so **27/45 cells contain a day that loses >= 100% of equity and 45/45 end below 1% of starting equity** - maxDD and the equity curve are therefore NOT usable columns in this batch. Sharpe IS leverage-invariant (mu/sigma cancels the scale), and so are gross PF, net PF, cost_R and R/trade; the verdict rests on those. The mechanism is stark: mean net R per trade reaches **-1.076 R** on the worst cells - the cost alone exceeds the entire risk unit. **DSR is NOT informative in this batch and is doing no work, stated so nobody quotes it.** The structural pool (this batch's own 45 a priori cells) has mean Sharpe **-20.01**, sd 2.86, E[max SR] **-13.61**; when every cell is catastrophic a merely less-catastrophic one can post a high-looking DSR while losing money on every trade. SURVIVOR requires net PF > 1 AND Sharpe > 0 as well, and both bind long before DSR does. The project-cumulative 544-Sharpe pool (E[max SR] +14.95) is printed for CONTRAST only - the exact sigma-contamination `research/dsr.py` BUG 2 documents. **OUT OF REGIME - and this is where M1 differs from all three previous candidates.** Index basket 1.363 -> 1.006, Sneaky Pivot 1.321 -> 1.155 (14/16 held), ORB 1.141 -> 0.960 (2/12): each had a real in-regime gross edge that then shrank or inverted. **M1 never had one to lose - mean gross PF is 0.996 in regime and 0.988 out of it, below 1.00 in BOTH windows.** It is the first candidate in this project to fail the gross test IN regime, so the out-of-regime run confirms rather than reveals. cost_R doubles out of regime (26.5% -> 57.1%) on the same stop-distance mechanism sections 9.4 and 10 found: median 1R falls 8.8 -> 6.1 bps in the low-vol 2013-2017 grind while the spread widens (1.03 -> 2.39 bps). **DATA CAVEAT:** US30 2013-2017 holds **1 bar of 467,543 (0.0002%) with a spread of exactly 0** (bid == ask at that minute's close; zero NEGATIVE spreads in any file). It hands one trade a free round-turn, which flatters the strategy - the safe direction for a kill - and `verify_m1.py` now caps zero-spread bars at 0.01% of a file rather than ignoring them. Files: `run_sweep_m1.py`, `run_sweep_m1_pre2018.py` (rebinds names only, no logic of its own), `scripts/verify_m1.py` (hard gate), `scripts/m1_gross_significance.py`, `scripts/probe_m1.py`, `scripts/smoke_test_m1.py`, `scripts/run_m1.sh`/`.cmd`. Results: `results/sweep_m1.csv`, `sweep_m1_scored.csv`, `sweep_m1_rth.csv`, `sweep_m1_rth_scored.csv`, `sweep_m1_pre2018.csv`, `sweep_m1_pre2018_scored.csv`, `m1_gross_significance.csv`, `m1_run.log`. Reproduce: `scripts/run_m1.cmd` (gate -> in regime -> matched control + out of regime, detached, ~13 min). |
| 2026-08-29/30 | **ORB IMPLEMENTATION AUDIT — verifying the kill, not re-testing the strategy** | NAS100, US30 | M1, RTH 09:30-16:00 ET | Five audits against `strategies/orb.py`/`run_orb.py`: (1) entry timing intrabar-vs-close, (2) fire-rate sanity check with per-filter day counts, (3) stop-first same-bar tie sensitivity, (4) whether the OR-width stop was ever cost-deliberate + one new MODERATE fixed-25bps-stop variant, (5) general code review. Tools: `scripts/audit_orb.py`, `scripts/audit_orb_tiebreak.py`, `scripts/run_orb_rerun.py`. | **The kill is real, not a code artefact.** (1) Entry is ALREADY intrabar (fires on M1 high/low touching the OR level, a resting-stop-order fill), not close-confirmed — the audited conservative assumption was never made; a close-only rule would have MISSED or delayed 41.6-45.9% of real breakouts (measured on 5,933 breakout bars across all 4 datasets), so the current code is the less conservative choice. (2) Fire rate 93.9-100% of valid opening ranges per instrument/year/OR, far above the 85-90% sanity bar in both windows; filtered days are negligible (truncated sessions 44-62/window = data completeness not a strategy filter, OR-incomplete 0-9, degenerate 0, same-minute ties 0-13) and true no-breakout days are 0-13. (3) Re-resolving all 30,840 trades (12 configs x 2 windows) under target-first instead of stop-first changed ZERO trades — `n_true_ties=0` in all 24 cells; the OR range (32-60 bps) always dwarfs a single M1 bar's range, so the tie never occurs. (4) Confirmed the OR-width stop was pure geometry, never cost-chosen. Added `stop_mode='moderate'`, `MODERATE_STOP_BPS=25` (solved from measured ~2.9-3.6 bps round-turn cost to land cost_R at the middle of a stated 10-15% band), same 2x2x3 breadth, 12 more cells both windows. Re-run (old 12 + new 12, both windows, same gates): **0/48 survivors.** In regime the moderate stop makes it WORSE (net PF>1 5/12->2/12, mean net Sharpe -0.153->-0.561) because a fixed round-turn cost eats a larger share of a now-smaller R. Out of regime both variants stay dead (gross PF>1 2/12->3/12 moderate, net PF>1 0/12 both). | Not a new trial batch for the DSR pool — this is a robustness/implementation audit of the already-killed §10 result, run against the SAME data and SAME gates, plus one new deliberately-designed stop variant (12 cells x 2 windows = 24 new configs, since it is a genuinely new a priori design choice, not a re-scoring). **Verdict: no code-level reason remains to doubt the §10 kill.** Every assumption audited was either already the non-conservative/realistic choice, provably inert on this data, or replaced with a deliberate alternative that made results worse, not better. STATE_OF_PLAY section 10.1-10.3 holds the full detail. Results: `results/audit_orb_fire_rate.csv`, `audit_orb_tiebreak.csv`, `orb_rerun_in_scored.csv`, `orb_rerun_out_scored.csv`, `audit_orb_run.log`, `audit_orb_tiebreak_run.log`, `orb_rerun_run.log`. Reproduce: `py -3.14 scripts/audit_orb.py`, `py -3.14 scripts/audit_orb_tiebreak.py`, `py -3.14 scripts/run_orb_rerun.py`. |
| 2026-08-30 | **ORB TREND-FILTERED VARIANT — one new gate on the audited engine** | NAS100, US30 | M1, RTH 09:30-16:00 ET | Reuses `strategies/orb.py`'s audited breakout detection, entry, target and `stop_mode='or_range'` UNCHANGED; adds ONE new causal filter: `daily_trend_direction()`, a 50-session SMA of the cash-session close (same definition on both windows), long breaks require the prior session's close above its own prior-only SMA, short breaks below, value assigned via explicit `.shift(1)`. Checked causal via the existing statistical look-ahead guard (24/24 PASS) AND a direct `assert_causal()` re-derivation from the unshifted sign(close-SMA) on every surviving candidate. Same 12-cell grid (2 inst x 2 OR x 3 targets) x 2 windows = 24 new configs, run alongside a same-run unfiltered recompute (`scripts/run_orb_trend.py`) that matches `results/orb.csv`/`orb_pre2018.csv`. | **Kill, for exactly the two predicted reasons — the filter fixes neither.** Filter removed ~half the trades (787-800/1,530-1,616 in regime, 472-529/970-1,025 out of regime); removed-vs-kept win rate and mean net R are close and sign-mixed across cells (e.g. US30 OR15 close in regime: removed mean net R +0.0295 vs kept -0.0751 — removed trades were the BETTER ones in that cell), so the filter is not simply cutting losers. Gates: gross PF>1 12/12->12/12 (in regime, unchanged) and **2/12->5/12** (out of regime, nudges up but mean gross PF stays 0.979 < 1.00); net PF>1 5/12->4/12 (in regime) and 0/12->0/12 (out of regime, unchanged); OOS holds 1/12->2/12; not-concentrated (top year<=60%) 0/12->1/12 in regime but **2 cells now show EXTREME concentration (top year >=100% of net R: NAS100 OR30 1R at 256%, OR30 2R at 103%)** that were not extreme unfiltered — the filter concentrates INTO trending years rather than spreading P&L, the predicted direction. beats B&H 0/12->0/12 both windows. DSR>0.95 0/12 both windows. **SURVIVORS 0/24.** | Cumulative trials **N=622** (598 prior [see correction below] + 24 trend-filtered, both windows). **Correction folded in this session:** the 2026-08-29/30 ORB moderate-stop audit variant (24 cells, STATE_OF_PLAY 10.1-10.3) was a genuine new a priori design choice that should have been added to the project's headline trial count (§1's Total table) at the time and was not — fixed now, 574->598->622, no verdict changes (both added batches scored 0 survivors). DSR structural pool = this batch's own 12 a priori cells per window: in regime E[max SR] +0.314 (mu -0.149, sd 0.278), out of regime E[max SR] -0.730 (mu -1.614, sd 0.531). **Reading: a trend filter that only decides WHICH SIDE of a breakout to take cannot, by construction, manufacture a pre-2018 gross edge that the raw price action does not have, and it cannot spread concentrated P&L across more years — if anything it makes concentration worse by construction (it survives more selectively into whichever years were already trending).** Both mechanisms were stated as expected failure modes before the run and both held. Files: `strategies/orb.py` (`daily_trend_direction`, `orb(..., trend_dir=...)`, additive, default `None` preserves section 10 byte-identically), `scripts/run_orb_trend.py`. Results: `results/orb_trend_in_regime_scored.csv`, `orb_trend_out_regime_scored.csv`, `orb_trend_*_unfiltered_reference.csv`, `orb_trend_run.log`. Reproduce: `py -3.14 scripts/run_orb_trend.py`. |
| 2026-08-30 | **CROSS-SECTIONAL MOMENTUM ROTATION — a portfolio-level, monthly-rebalance strategy, structurally different from every prior candidate** | 11 SPDR sectors (XLK XLF XLE XLV XLI XLY XLP XLU XLB XLRE XLC) + 6 asset-class ETFs (TLT GLD IEF IWM EFA EEM), benchmark SPY | **Daily bars, monthly rebalance** | New data source: yfinance daily adjusted close, `period="max"`, verified real OHLCV with per-ticker actual start dates (9 sector SPDRs + SPY from 1998-12/1993-01; XLRE 2015-10; XLC 2018-06; TLT/IEF 2002-07; GLD 2004-11; IWM 2000-05; EFA 2001-08; EEM 2003-04) — no gaps beyond calendar weekends/holidays, no backfilling. Strategy: monthly rebalance on the last trading day actually present each month; rank the 17-ETF universe (SPY excluded, benchmark-only) by trailing N-month total return (**N=6, N=12**, month-end-close to month-end-close); hold the top **K=3** or **K=5**, equal-weighted; market filter goes 100% to IEF whenever SPY's close is below SPY's own causal 200-day SMA at the signal date. **4-cell grid** (N x K), no tuning beyond it. Causality: signal at close(t) (last trading day of month) -> trade modelled as executed at close(t+1) -> first live return earned is close(t+2)/close(t+1)-1, implemented via `weights_daily.shift(1) . daily_returns` so no rebalance can use its own signal date's forward return. Costs: 2 bps/side spread + 1 bps/side commission-equivalent = 3 bps/side (6 bps round-turn) on turnover, applied even though monthly rebalancing is low-turnover. `research/momentum_rotation.py` (engine) + `run_momentum_rotation.py` (driver). | **KILL — 0/4 survive, but this is the first candidate whose GROSS edge behaves exactly as the structural hypothesis predicted rather than inverting or collapsing.** Full period (1998/2000-2026, per-ticker inception-limited): net Sharpe **0.514-0.541** across all 4 cells (a genuinely tight cluster — the specific N/K choice barely matters), net CAGR 5.9-6.7%, maxDD 32.0-44.6%, **cost = only 5.0-7.1% of gross return — confirms explicitly, as expected, that monthly rebalancing is a structurally different cost regime from every intraday candidate in this project** (5.7-60.5% cost_R elsewhere). Stress window (2000-01-01 -> 2009-12-31, SAME 4 configs unchanged): the market filter earns its keep — net Sharpe **0.562-0.696**, i.e. HIGHER than the full-period Sharpe, because it sidesteps most of the dot-com crash and 2008 (SPY buy-and-hold itself manages only Sharpe **0.071**, CAGR **-0.52%** over that decade). **Gates:** DSR>0.95 **0/4 full, 0/4 stress** (best full DSR 0.500 against E[maxSR] +0.541 on this batch's own 4-cell structural pool — the tight Sharpe cluster means even a 4-trial pool sets a bar the best cell cannot clear); top-year concentration **4/4 PASS** (12.4-13.8% of full-period log-return, no single-year dependency — unlike every prior candidate in this project, concentration is NOT what kills this one); **beats SPY buy-and-hold: full period 0/4 (net Sharpe 0.51-0.54 vs SPY 0.650), stress window 4/4 (0.56-0.70 vs SPY 0.071)** — the strategy loses to the index in the easy regime and beats it decisively in the hard one, the reverse of every FTMO/breakout candidate this project has tested. **SURVIVORS 0/4** because the full-period DSR and full-period beat-SPY gates both fail for every cell. | Cumulative trials **N=630** (622 prior + 4 FULL + 4 STRESS; the stress window is a new sealed test on the same 4 a priori configs, counted as new trials per this project's convention for out-of-regime windows, e.g. section 6/9.3/10/11). Look-ahead guard: PASS on all 4 cells (execution date strictly after signal date by construction; explicit assertion in `look_ahead_guard()`). Stress-window universe limits stated plainly, not backfilled: XLRE and XLC did not exist yet (absent from ranking entirely, 2000-2009); TLT/IEF/EFA/EEM/GLD phase in mid-window at their real inception dates and are simply unavailable to be ranked before then — exactly as a real 2000 portfolio manager would have experienced it. **Reading: this is a structurally different result from every other candidate in this project** — its cost story is a non-issue as predicted (portfolio-level monthly rebalancing has ~10x lower cost_R than any intraday family tested), its P&L is NOT single-year concentrated (the first candidate to clear that gate at all), and it genuinely outperforms buy-and-hold in the stress regime that killed the macross basket, Sneaky Pivot and ORB. **It is still killed** because (a) it underperforms SPY in the easy 1998-2026 regime that supplies most of the sample, and (b) the DSR gate cannot be cleared even against its own tiny 4-cell pool — the N/K grid is so flat (Sharpe 0.51-0.54 across all four) that no single cell stands out from what a 4-trial null would produce anyway, meaning the FINDING (monthly cross-sectional rotation + a 200-day trend filter genuinely helps in equity stress regimes) is more robust than any single CONFIG within it, and robustness-without-standout-Sharpe is exactly what DSR is built to refuse to reward. Files: `scripts/download_momentum_universe.py`, `research/momentum_rotation.py`, `run_momentum_rotation.py`. Data: `data/momentum_universe_adjclose.csv`, `data/*_daily_yfinance.csv`, `data/momentum_universe_report.csv`. Results: `results/momentum_rotation_configs.csv`, `momentum_rotation_summary.txt`, `momentum_rotation_run.log`. Reproduce: `python scripts/download_momentum_universe.py && python run_momentum_rotation.py`. |
| 2026-08-30 | **MOMENTUM ROTATION AUDIT — a real bug found and fixed, verdict narrows but does not flip; widened universe tested separately, verdict unchanged** | 17-instrument base + 27-instrument widened universe, SPY benchmark | Daily, monthly rebalance | Five audits against `research/momentum_rotation.py`/`run_momentum_rotation.py`: (1) volatility mismatch vs SPY + vol-matched (constant-leverage) comparison, (2) risk-free rate consistency between rotation and SPY Sharpe, (3) total-return correctness (auto_adjust=True applied identically to SPY and IEF, not just the ranked universe), (4) survivorship stated as a limitation, (5) general code review. Then, as a SEPARATE labeled step: widened the universe with 10 new tickers (DBC, USO, UNG, SLV, VGK, INDA, FXI, MTUM, VTV, MDY — commodities, international, factor, mid-cap breadth), same 4-cell grid, same causal logic, full period + 2000-09 stress. Tools: `scripts/audit_momentum_rotation.py`, `scripts/download_momentum_universe_expanded.py`, `run_momentum_rotation_expanded.py`. | **Audit 2 (risk-free) and Audit 3 (total return) both PASS**, confirmed not just read: both the rotation and SPY Sharpe use the identical `sharpe()` function with zero risk-free subtraction on either side; SPY and IEF adjusted/raw close ratios (0.65 and 0.50 respectively, measured 2003-01 to 2003-05) confirm dividend/coupon reinvestment is baked into both series via the same code path as the ranked universe. **Audit 4 (survivorship)** stated as an unfixed, known limitation — the universe contains only funds that exist today. **Audit 5 (code review) found a real bug**: `compute_metrics()` computed "full period" Sharpe/vol/CAGR over the ENTIRE 1993-2026 panel (8,453 obs), but no config can trade before it has enough lookback history — first live rebalance is 1999-07-01 (N=6) or 2000-01-03 (N=12) — and `simulate()` fills exact-zero returns before that date, which are real rows (not NaN) that entered every full-period statistic. This **pads volatility and CAGR down** (a conservative-direction bug, but a real one). **Audit 1, recomputed on the corrected live-window-only period**: the vol mismatch is real and confirmed (strategy runs 19-28% lower annualized vol than SPY on the identical window, 13.9-15.8% vs 19.2-19.3%), AND the correction **reverses the original "loses to SPY" full-period Sharpe finding**: corrected Sharpe 0.578-0.608 **beats** SPY's matched-window Sharpe 0.511-0.518 in **all 4 configs unlevered** (was 0/4 in the original padded computation, 0.514-0.541 vs SPY's mismatched-window 0.650). A constant-leverage vol-match (SPY_vol/strategy_vol, no re-optimization) makes vol-matched CAGR **beat SPY CAGR in all 4 full-period configs** (9.71-10.36% vs SPY's 8.36-8.43%) **and all 4 stress-window configs** (10.63-14.08% vs SPY's -0.91%, a 10.9-15.0pp/yr margin). DSR recomputed on the corrected Sharpes: still **0/4 clear 0.95 in either window** (best 0.5053 full, 0.4889 stress) — the grid remains too flat (0.578-0.608 spread) for DSR to reward any single cell. **Corrected verdict: still KILL, but only because of DSR — the "loses to buy-and-hold" reason in the original write-up was the bug, not a strategy fact.** **Widened-universe test (separate step, live-window methodology from the start)**: 27 instruments, same 4-cell grid. Full period: 3/4 configs beat SPY Sharpe (N=6,K=3 is the exception, 0.479 vs SPY 0.518); stress window: 4/4 beat SPY Sharpe (0.640-0.816 vs SPY 0.067). DSR still 0/4 both windows (best 0.5279 full, 0.5276 stress). **Widening the universe does not change the verdict** — same DSR ceiling, same sole blocking gate. | Audit itself is NOT a new trial batch — re-verification and bug-fix of section 12's existing 8 trials, run against the same data and configs; cumulative count unchanged by the audit (**N=630**). Widened-universe grid IS a new a priori design choice (a different, wider instrument set): **+8 trials, 630 -> 638.** Vol-matched leverage (1.22-1.44x across cells) is a simple constant multiplier with **no financing cost or overnight/gap-risk modeling** — same unmodeled-leverage caveat STATE_OF_PLAY section 2 raised for the old macross basket lead; this shows the volatility-adjusted comparison is fair, not that a levered ETF rotation is a deployable product as specified. Files: `scripts/audit_momentum_rotation.py`, `scripts/download_momentum_universe_expanded.py`, `research/momentum_rotation.py` (gained an optional `universe` parameter, additive, default preserves section 12 byte-identically), `run_momentum_rotation_expanded.py`. Data: `data/momentum_universe_expanded_adjclose.csv`, `data/{DBC,USO,UNG,SLV,VGK,INDA,FXI,MTUM,VTV,MDY}_daily_yfinance.csv`. Results: `results/momentum_rotation_audit_full.csv`, `momentum_rotation_audit_stress.csv`, `momentum_rotation_audit_run.log`, `momentum_rotation_expanded_full.csv`, `momentum_rotation_expanded_stress.csv`, `momentum_rotation_expanded_run.log`. STATE_OF_PLAY sections 12.1-12.2 hold the full detail including per-config tables. Reproduce: `python scripts/audit_momentum_rotation.py`; `python scripts/download_momentum_universe_expanded.py && python run_momentum_rotation_expanded.py`. |
| 2026-08-30 | **MOMENTUM ROTATION SECOND AUDIT — 6 more checks, no new bugs, filter perturbation and cost sensitivity both ROBUST** | 17-instrument base + 27-instrument widened universe, SPY benchmark | Daily, monthly rebalance | A second, independent audit assuming the first (5 checks, 1 bug found) did not catch everything. Audit 6: git history (`git log --all` on the strategy files, only 2 commits ever) + session record confirm the N/K/SMA/rebalance grid matched the task prompt verbatim in the FIRST commit — chosen before any result was seen, HIGH confidence not proof. Audit 7: all 27 tickers' `first_valid_index()` cross-checked against externally-sourced (web search, independent of the download script) real fund inception dates. Audit 8: one-shot perturbation test on N=12/K=5 (base universe) — 150d SMA, 250d SMA, bi-monthly rebalance, vs the original 200d/monthly. Audit 9: cost bracket 3bps/6bps/12bps round-turn (half/original/double) on the same cell. Audit 10: survivorship sized via known corporate-action history of all 27 tickers. Audit 11: DSR pool re-derived explicitly one more time from the corrected live-window Sharpes. Tool: `scripts/audit_momentum_rotation_2.py`. | **No new bug found.** Audit 6: only 2 commits ever touched this code; the first already contains the exact grid (SMA_WINDOW=200, monthly, N∈{6,12}, K∈{3,5}) matching the task prompt verbatim — no other value was ever coded or discarded. Stated as high confidence, not proof (unsaved interactive exploration can't be fully ruled out from git alone; any such doubt would only make the true trial count larger and DSR harder to clear, pushing toward the kill, not away). Audit 7: **all 27 gaps between known inception and data-first-valid are ≥0** (range +0d to +13d — SPY +7d, sector SPDRs +6d, EFA +13d largest), consistent with legal-inception-vs-first-trading-day lag, zero evidence of pre-inception/backfilled data anywhere. Audit 8: **ROBUST.** Full-period Sharpe spans 0.566-0.639 and stress Sharpe 0.523-0.610 across all 4 variants (original + 150d SMA + 250d SMA + bi-monthly) — none flips sign, none collapses, 150d SMA is actually slightly BETTER than the original. Audit 9: **ROBUST.** Doubling round-turn cost from 6bps to 12bps moves full-period Sharpe by only -0.012 and the vol-matched CAGR margin over SPY by only -0.25pp (full period: +2.00pp→+1.75pp; stress: +12.73pp→+12.57pp) — monthly rebalancing's low turnover means cost was never close to the margin, exactly as predicted. Audit 10: none of the 27 tickers has ever been delisted/merged/liquidated; the one identified corporate action (FXI's 2011 rename, same ticker/fund/series) has zero effect on the backtest; residual bias is universe-selection (a discontinued competitor fund unknown to this audit), sized as small not proven zero. Audit 11: **pool values reconfirmed identical to audit 1** — FULL `[0.595921, 0.581640, 0.577558, 0.607983]`, STRESS `[0.695629, 0.684052, 0.562532, 0.610368]`, DSR to 4 decimal places matches (best full 0.5053, best stress 0.4889), no staleness. | Not a new trial batch — audits 6/7/10/11 are pure verification and audits 8-9's one-shot perturbation/sensitivity runs are explicitly excluded from the DSR pool (same treatment as the ORB implementation audit, §10.1-10.3). Cumulative trial count unchanged: **N=638**. **Reading: this is now the most thoroughly-audited result in the project** — two independent audits, eleven checks total, one real bug found and fixed (§12.1, narrowing not flipping the kill), and the two checks that actually stress-test the finding rather than re-verify arithmetic (filter-design perturbation and a 4x cost bracket) both came back ROBUST rather than fragile. The mechanism — a long-SMA defensive filter on a monthly cross-sectional rotation — is not a knife's-edge accident tied to exactly 200 days and exactly monthly; it holds across the whole reasonable neighbourhood of that design and survives costs far outside any realistic range for these instruments. **This does not make it DSR-significant** — the entire neighbourhood of reasonable configs clusters in the same narrow ~0.52-0.64 Sharpe band, which is precisely why no single cell is a statistical outlier against even a tiny a priori pool; that is a property of the mechanism's flatness, not something further auditing can fix. Verdict unchanged: **KILL, on DSR alone.** STATE_OF_PLAY section 12.3 holds the full detail including all tables. Files: `scripts/audit_momentum_rotation_2.py`, `research/momentum_rotation.py` (gained additive optional `sma_window`/`rebalance_step` params on `build_weights()`, defaults reproduce sections 12/12.1/12.2 byte-identically). Results: `results/momentum_rotation_audit2_inception.csv`, `momentum_rotation_audit2_perturbation.csv`, `momentum_rotation_audit2_cost_sensitivity.csv`, `momentum_rotation_audit2_run.log`. Reproduce: `python scripts/audit_momentum_rotation_2.py`. |

